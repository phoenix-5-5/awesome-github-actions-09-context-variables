name:  Getting Started

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:      
      - name: Echo github
        run: |
          echo "Masked: "
          echo ${{ secrets.USERNAME }}    
          echo "Unmasked: "
          echo ${{ secrets.USERNAME }} | sed 's/./& /g'
